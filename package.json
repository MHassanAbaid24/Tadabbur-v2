{
  "name": "tadabbur-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root orchestration for Tadabbur Frontend and Backend",
  "scripts": {
    "dev": "npx concurrently -n \"frontend,backend\" -c \"cyan,green\" \"npm --prefix frontend run dev\" \"cd backend && ./venv/bin/uvicorn main:app --reload --port 8000\""
  }
}
